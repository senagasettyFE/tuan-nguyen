export interface Filters {
    widgetId: number;
    filters: any;
}

export interface MarketShareFilters {
    viewBy: string;
    // minYear: any;
    // maxYear: any;
    // startYear: any;
    // endYear: any;
}

export interface AverageProductionFilters {
    propertyTypes: string[];
}

